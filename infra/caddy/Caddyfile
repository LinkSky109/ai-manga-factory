{
	auto_https off
	admin off
}

:8080 {
	encode gzip zstd

	route {
		@api path /api/* /health /metrics
		handle @api {
			reverse_proxy api:8000
		}

		handle {
			root * /srv
			try_files {path} /index.html
			file_server
		}
	}
}
