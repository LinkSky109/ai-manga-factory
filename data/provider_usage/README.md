# provider_usage

模型用量监控与自动切换的持久化目录。

- `model_budget_config.json`：模型预算、优先级、预警阈值和切换阈值配置
- `usage_ledger.json`：运行时累计用量、最近错误、最近切换事件

说明：

- 文本模型的用量按本地估算 `tokens` 统计，不是供应商官方账单。
- 图片和视频模型的用量按调用次数统计。
- 当某个模型预计用量达到 `switch_ratio`，或者出现额度/欠费类错误时，系统会自动尝试后备模型。
- 预算值需要按你的真实免费额度或套餐额度调整，否则“接近上限”的判断只是一层本地守门。
